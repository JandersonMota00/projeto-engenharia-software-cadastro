meta {
  name: login
  type: http
  seq: 3
}

post {
  url: {{host}}/login/
  body: json
  auth: none
}

body:json {
  {
    "username": "useruser",
    "password": "userpassword"
  }
}

script:post-response {
  bru.setEnvVar("auth-token",res.body.token)
}
